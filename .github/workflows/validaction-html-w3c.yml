name: Validar HTML con W3C

on:
  push:
    branches:
      - main

jobs:
  validate-html:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repo
      uses: actions/checkout@v2

    - name: Validar HTML con W3C
      uses: falnyr/W3C-Validator@v1.0.0
      with:
        targets: '*.html'
